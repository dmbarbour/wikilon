# Stowage

Stowage is a concrete, LMDB-based implementation of the abstract `Stowage` and `DB` types from the Stowage.Data package. This is intended to be the primary implementation of Stowage, although it's feasible to support Stowage through cloud services.

